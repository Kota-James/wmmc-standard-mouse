# 環境構築ガイド（Windows / macOS / Ubuntu）

新入生が最初にやる環境構築を OS 別にまとめる。
迷ったら部で一番ユーザーが多い **Windows + WSL** の構成を勧める。

## 0. 必要なものの全体像

| ツール | 用途 | Windows | macOS | Ubuntu |
|---|---|---|---|---|
| STM32CubeIDE | ファームのビルド・書き込み・**デバッグ**、`.ioc` の編集 | Windows 版 | mac 版 | Linux 版 |
| Git | バージョン管理（[Git入門](Git入門.md)） | Git for Windows | Xcode CLT に同梱 | apt |
| シリアルターミナル | printf の閲覧（115200bps） | Tera Term | screen（標準） | screen など |
| gcc + make | シミュレータ（`Sim/`）のビルド | WSL 内に導入 | Xcode CLT に同梱 | apt |
| arm-none-eabi-gcc | コマンドラインでのファームビルド（§4） | WSL 内に導入 | brew | apt |

CubeIDE のダウンロードには ST のアカウント登録（無料）が必要。

> **CubeIDE を入れずに始めることもできる。** `arm-none-eabi-gcc` + `make` でビルドし、
> できた `.bin` を Nucleo ボードにドラッグ&ドロップすれば書き込める（**§4**）。
> インストールが軽く、Import やデバッグ設定でつまずかないので、まずこれで
> 課題を進めて、ステップ実行が必要になったら CubeIDE を入れる進め方でよい。

---

## 1. Windows（推奨: CubeIDE は Windows 側、Sim は WSL 側）

### 1-1. Windows 側

1. [STM32CubeIDE](https://www.st.com/ja/development-tools/stm32cubeide.html) をインストール
2. [Git for Windows](https://gitforwindows.org/) をインストール（設定はすべて既定値でよい）
3. [Tera Term](https://teratermproject.github.io/) をインストール
   （マウス接続後、シリアルポート → ST-Link の COM ポート → 115200bps）

### 1-2. WSL（Ubuntu 24.04）側 — シミュレータ用

PowerShell（管理者）で:

```powershell
wsl --install -d Ubuntu-24.04
```

再起動後、Ubuntu のターミナルで（ここから下は sudo が必要）:

```sh
sudo apt update
sudo apt install -y build-essential git
# コマンドラインでのファームビルド用（§4 の方法を使うなら必須）
sudo apt install -y gcc-arm-none-eabi libnewlib-arm-none-eabi
```

リポジトリは WSL 側（`~/` 以下）にクローンし、シミュレータは WSL で動かす。
CubeIDE から同じフォルダを開くときは `\\wsl$\Ubuntu-24.04\home\<ユーザー名>\...`
を Import すればよい（Windows 側に別途クローンしても構わないが、二重管理に注意）。

> **書き込みは Windows 側から行う。** CubeIDE で書き込むか、WSL で `make` して
> できた `.bin` をエクスプローラーからボードにコピーする（§4-2）。
> どちらも Windows 側が USB を握るので問題ない。
> WSL から ST-Link を直接叩く（`STM32_Programmer_CLI` 等）には usbipd-win による
> USB パススルーが必要で上級者向け。新入生はやらなくてよい。

---

## 2. macOS

1. コマンドラインツール（gcc / make / git がまとめて入る）:
   ```sh
   xcode-select --install
   ```
2. [STM32CubeIDE](https://www.st.com/ja/development-tools/stm32cubeide.html) の
   macOS 版をインストール（Apple Silicon / Intel とも可）
3. シリアルは標準の `screen` で見られる（終了は `Ctrl-a` → `k` → `y`）:
   ```sh
   ls /dev/tty.usbmodem*        # マウス接続後にポート名を確認
   screen /dev/tty.usbmodem<番号> 115200
   ```
4. コマンドラインでのファームビルド用（§4 の方法を使うなら必須）:
   ```sh
   brew install --cask gcc-arm-embedded
   ```

---

## 3. Ubuntu 24.04（ネイティブ）

1. 基本ツール:
   ```sh
   sudo apt update
   sudo apt install -y build-essential git
   # コマンドラインでのファームビルド用（§4 の方法を使うなら必須）
   sudo apt install -y gcc-arm-none-eabi libnewlib-arm-none-eabi
   ```
2. [STM32CubeIDE](https://www.st.com/ja/development-tools/stm32cubeide.html) の
   Linux 版（.deb）をインストール。インストーラが ST-Link の udev ルール
   （USB デバイスへのアクセス許可）も設定してくれる
3. **シリアルポートの権限**（Linux で一番ハマる箇所）:
   ```sh
   sudo usermod -aG dialout $USER
   ```
   を実行して**一度ログアウト→ログイン**する。これをしないと
   `/dev/ttyACM0` が Permission denied になる
4. シリアルの閲覧:
   ```sh
   screen /dev/ttyACM0 115200
   ```

---

## 4. （代替手段）CubeIDE を使わずにビルド・書き込みする

CubeIDE の Import やデバッグ設定でつまずいたときは、こちらの方が早い。
**書き込みだけならソフトのインストールは arm-none-eabi-gcc と make のみで足りる。**

### 4-1. ビルド

リポジトリ直下の `Makefile` は CubeIDE の Debug 構成と同じコンパイルオプションを
再現している（CubeIDE の GUI ビルドとは出力先が別なので、併用しても衝突しない）。

```sh
make          # build/wmmc-standard-mouse.elf と .bin ができる
make clean    # build/ を消す
```

### 4-2. 書き込み（ドラッグ&ドロップ）

Nucleo ボードの ST-Link は **USB メモリとしても見える**。
そこに `.bin` をコピーするだけで書き込める。

1. マウスを USB で PC につなぐ
2. `NODE_F303K8` のようなリムーバブルディスクが現れる
   （ST-Link のファーム版によっては `NOD_F303K8` などに省略される）
3. `build/wmmc-standard-mouse.bin` をそのドライブにコピーする
4. コピーが終わると自動で書き込まれ、ボードが再起動する

WSL でビルドした場合は、エクスプローラーのアドレスバーに
`\\wsl$\Ubuntu-24.04\home\<ユーザー名>\wmmc-standard-mouse\build`
と入力すればそのフォルダを開けるので、そこからドライブへドラッグ&ドロップする。

> コピー後にドライブに `FAIL.TXT` ができていたら書き込み失敗。
> 中身にエラー理由が書かれている。`.elf` ではなく **`.bin`** を渡しているか確認する。

### 4-3. この方法の限界

- **ブレークポイントを使ったデバッグはできない。** printf（UART 115200bps）で追う
- `.ioc` を編集してコード生成し直す作業には CubeMX（CubeIDE 内蔵）が必要
- ステップ実行したくなったら CubeIDE を入れる。それまではこの方法で進めてよい

---

## 5. sudo（管理者権限）が必要な操作のまとめ

新入生が `sudo` を打つのは以下だけ。**それ以外で sudo を要求される手順に
なったら何かがおかしいので先輩に相談**すること。

| OS | コマンド | 目的 |
|---|---|---|
| WSL / Ubuntu | `sudo apt install ...` | ツールの導入 |
| Ubuntu | `sudo usermod -aG dialout $USER` | シリアルポートの読み書き権限 |
| Ubuntu | CubeIDE の .deb インストール | IDE 本体と udev ルール（§4 の方法なら不要） |
| Windows | `wsl --install`（管理者 PowerShell） | WSL の導入 |

---

## 6. 動作確認チェックリスト

環境ができたら [課題0](exercises/00_開発環境とLチカ.md) に進む前に確認:

- [ ] `git --version` が表示される
- [ ] ファームのビルドが通る（CubeIDE の Import → Build、または §4 の `make`）
- [ ] マウスを USB 接続して書き込みができる
- [ ] シリアルターミナルに起動バナーが出る
- [ ] `cd Sim && make` がエラーなく終わる

> シミュレータは**ビルドが通ることだけ**を確認する。配布時点では課題4（`maze.c`）と
> 課題5（`solver.c`）が未実装なので、`./sim mazes/empty.maze` を実行すると
> 「不正な経路データ」「探索走行 失敗」になり `make test` も全 FAIL する。
> **これは正常**で、課題4・5 を実装すると PASS に変わる。

## 7. よくあるつまずき

| 症状 | 原因と対処 |
|---|---|
| 書き込めない | USB ケーブルが充電専用（通信線なし）。ケーブルを変える |
| ドライブが現れない（§4-2） | ケーブルが充電専用 / ST-Link のファームが古い。CubeIDE か STM32CubeProgrammer の ST-Link Upgrade で更新する |
| コピーしたら `FAIL.TXT` ができた（§4-2） | `.elf` を渡している（`.bin` を渡す）。中身にエラー理由が書かれている |
| シリアルに何も出ない | ボーレートが 115200 以外 / ポート選択ミス / （Ubuntu）dialout 権限（§3-3） |
| `./sim mazes/empty.maze` が「失敗」になる | 課題4・5 が未実装なら**正常**（§6 の注記）。実装後も失敗するなら実装のバグ |
| WSL で `./sim` が動くのに Windows で動かない | Sim は WSL（または mac/Linux）用。Windows 側で動かす必要はない |
| 日本語コメントが文字化けする | CubeIDE の Text file encoding を UTF-8 に（課題0参照） |
| `make: command not found` | §1-2 / §2-1 / §3-1 のツール導入を飛ばしている |
| `arm-none-eabi-gcc: command not found` | §1-2 / §2-1 / §3-1 の arm-none-eabi-gcc の導入を飛ばしている（§4 の方法には必須） |
